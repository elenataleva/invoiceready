ANTHROPIC_API_KEY=sk-ant-placeholder
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/invoiceready
ENV=development
# Comma-separated CORS allowlist. Defaults (in app/config.py) cover local
# dev; a deployed instance must set this to its real frontend origin(s).
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8000
