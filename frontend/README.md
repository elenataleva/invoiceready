# InvoiceReady frontend

React SPA (Vite + TypeScript + Tailwind v4 + shadcn/ui), per `docs/04-FRONTEND-DESIGN.md`.

See the [root README](../README.md) for the project as a whole. This file stays short on purpose - per the design doc, one README is better for a portfolio than two.

```
nvm use          # Node version pinned in .nvmrc
npm install
npm run dev       # http://localhost:5173
npm run build
npm run lint
```
